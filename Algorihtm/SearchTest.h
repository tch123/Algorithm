#pragma once

namespace Vonng
{
    namespace Test
    {
        void generateTestData(size_t N, size_t M);
        double testBinarySearch();
        double testLinearSearch();
        void testSearch();
    }
}